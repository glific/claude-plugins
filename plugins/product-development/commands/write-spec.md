---
description: Write a feature spec from an idea, prototype URL, PRD, or design doc — or scope a versioned iteration from an existing spec
---

Use the `write-spec` skill to write or scope a product feature specification.

User input (may be a feature description, a URL to a prototype/PRD, a path to notes, or a path to an existing `features/` folder for versioning):

$ARGUMENTS
